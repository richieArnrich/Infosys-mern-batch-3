//import all the functions given by functions.js
const funcs = require("./functions");

console.log(funcs);

funcs.add(12, 13);
funcs.sub(14, 7);
funcs.multiply(14, 5);
let div = funcs.divide(10, 2);
console.log(div);

// the script used to write js code
// is ecma script
// it is a superset of the ECMAScript standard
